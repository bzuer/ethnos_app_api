# Test Environment Configuration - DO NOT USE PRODUCTION CREDENTIALS
NODE_ENV=test
PORT=3001

# Test Database Configuration (use test database or mock)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=data_db
DB_USER=api_dev
DB_PASSWORD=s0urc3_v3nv

# Redis Configuration (optional for tests)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Test Logging Configuration
LOG_LEVEL=silent
LOG_FILE=logs/test.log

# Relaxed Rate Limiting for Tests
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10000
RATE_LIMIT_GENERAL=10000
RATE_LIMIT_SEARCH=5000
RATE_LIMIT_DOWNLOAD=2000
RATE_LIMIT_METRICS=3000
RATE_LIMIT_RELATIONAL=2000

# Test Cache Configuration (shorter TTL for faster tests)
CACHE_TTL_SEARCH=60
CACHE_TTL_STATISTICS=300
CACHE_TTL_WORK_DETAILS=120
CACHE_TTL_PERSONS=120
CACHE_TTL_ORGANIZATIONS=120

# Test Pagination Configuration
DEFAULT_LIMIT=20
MAX_LIMIT=100

# Disable security features that might interfere with testing
ENABLE_RATE_LIMITING=false
ENABLE_HELMET=false
ENABLE_CORS=true
CORS_ORIGIN=*

# Test-specific configurations
DEBUG_SQL=false
ENABLE_REQUEST_LOGGING=false
LOG_PERFORMANCE=false

# Search engine disabled in tests to stabilize suite
SPHINX_ENABLED=false

# Disable Sphinx for /venues listing specifically during tests
VENUES_SPHINX_ENABLED=false

# Fail-fast DB timeouts for tests
DB_QUERY_TIMEOUT_MS=4000
REQUEST_TIMEOUT_MS=5000

# Internal Access Keys for protected endpoints
SECURITY_ACCESS_KEY=test-security-key
INTERNAL_ACCESS_KEY=test-internal-key
