Database directory

Purpose
- Keep schema and lightweight SQL snippets related to development.

Do not store production data dumps here
- Large SQL dumps and binary data must not be committed.
- Use external backup storage and operational runbooks for data lifecycle.

See also
- `data_db_schema.sql` in repo for reference schema snapshots.
- Scripts under `scripts/` for maintenance and data checks.
